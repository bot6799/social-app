import {utils} from '@bsky.app/alf'

export const BLUE_HUE = 220 // Europe blue #003399

/**
 * @deprecated use `utils.alpha` from `@bsky.app/alf` instead
 */
export const transparentifyColor = utils.alpha
